apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvcClaim }}
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
